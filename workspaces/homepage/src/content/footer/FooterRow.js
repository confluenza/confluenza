import styled from '@emotion/styled'

const FooterRow = styled.div({
  width: '100%',
  display: 'flex',
  flexFlow: 'row nowrap',
  justifyContent: 'flex-start',
  alignItems: 'center',
  marginBottom: '30px'
})

export { FooterRow }
